/**
 * 桌面应用下载页 URL。
 *
 * 注意：源码仓库（viile/curl_display）是私有的，对外用户访问会 404。
 * 我们另起一个公开仓库 viile/curl_runner 只承载 README + Releases，
 * 由 .github/workflows/release.yml 在打 tag 时跨仓库推送安装包。
 * 因此对外展示的下载地址应当指向公开仓库的 releases/latest。
 *
 * 如果你 fork 后用自己的 repo，改这里即可。
 */
export const DESKTOP_DOWNLOAD_URL =
  'https://github.com/viile/curl_runner/releases/latest';
